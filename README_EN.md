# Design and Deployment of an Ethernet-Enabled Embedded Access Control Platform on a Renesas Synergy MCU

Main project documentation for `IoTRamoRenesas`.

Last review of this documentation: `2026-05-09`

## 1. Overview

This project implements an access-control system for the **Renesas SK-S7G2** board, with:

- RFID card reading through RC522
- local interface on TFT display with touch
- user authentication and door opening
- administration through local web interface
- profile, photo, and log persistence in QSPI/FileX
- NTP time synchronization to store logs with real timestamps

The project evolved on top of a codebase sensitive to memory, boot sequencing, and FileX behavior. Because of this, several current decisions prioritize stability and separation of responsibilities.

## 2. Current state

Working today:

- RFID with authentication and basic local enrollment
- display, touch, and local UI
- administrative web panel
- profiles with name, role, chapter, photo, and multiple cards
- high-resolution runtime photo upload from web with persistence in QSPI
- automatic profile persistence in QSPI
- access log persisted in QSPI
- DHCP to obtain IP automatically
- clock synchronization via NTP
- token-protected HTTP API for remote door opening

Important points about the current state:

- network runs in `HTTP only` mode
- local and web admin authentication accepts PINs from administrator profiles
- PIN `1234` remains enabled as the default fallback
- door flow records opening only, not closing
- screen dims visual brightness to about `30%` after `60s` of inactivity
- door and light relays are explicitly configured as GPIO at boot

## 3. Hardware and peripherals

### 3.1 Platform

- Board: `Renesas SK-S7G2`
- RTOS: `ThreadX`
- Network/IP/HTTP: `NetX / NetX Duo`
- File system: `FileX`
- Persistent storage: `QSPI`

### 3.2 RFID

RC522 is connected through software SPI.

Current mapping in [`src/rfid.c`](./src/rfid.c):

- `D10 -> SS`  (`P507`)
- `D9  -> RST` (`P506`)
- `D11 -> MOSI` (`P105`)
- `D12 -> MISO` (`P104`)
- `D13 -> SCK`  (`P106`)

Notes:

- RC522 SPI is bit-banged
- there is support for 4, 7, and 10-byte UIDs
- reader works in polling mode

### 3.3 Display and touch

Display:

- controller initialized in [`src/hardware/lcd_setup.c`](./src/hardware/lcd_setup.c)
- panel based on `ILI9341` initialization sequence
- logical area used by UI: `240x320`

Touch:

- `SX8654` controller
- pins defined in [`src/ui.c`](./src/ui.c):
  - `SDA -> P511`
  - `SCL -> P512`
  - `IRQ -> P004`
  - `RESET -> P609`

### 3.4 Door, light, and buttons

Current mapping in [`src/gpio.c`](./src/gpio.c):

- door relay: `D6 -> P613`
- light relay: `D5 -> P608`
- door button: `P008`
- external door button: `D4 -> P112`
- light button: `P009`

Behavior:

- door opens with a timed `2s` pulse
- automatic closing does not generate a log event
- the `D4` button acts as an additional external door trigger while the UI is idle
- local buttons can also trigger UI events
- relay pins are configured manually in `gpio_init()`, instead of relying on `pin_data.c`

## 4. Repository structure

Main directories:

- [`src`](./src): application code
- [`src/assets`](./src/assets): logo, fonts, and photo assets
- [`src/hardware`](./src/hardware): display-specific hardware initialization
- [`src/synergy_gen`](./src/synergy_gen): code generated by e2 studio/SSP
- [`synergy`](./synergy): BSP and Synergy/Azure RTOS components
- [`synergy_cfg`](./synergy_cfg): project support configurations
- [`script`](./script): scripts for import, conversion, and asset generation
- [`Debug`](./Debug): development build output

Support files:

- [`CURRENT_STATE_EN.md`](./CURRENT_STATE_EN.md): quick operational snapshot
- [`NETWORK_NOTES_EN.md`](./NETWORK_NOTES_EN.md): network investigation history
- [`configuration.xml`](./configuration.xml): project stack/config in e2 studio

## 5. Software architecture

## 5.1 Main modules

- [`src/main.c`](./src/main.c)
  - global application state
  - event queue
  - in-RAM access log
  - software clock based on UTC synchronized by NTP
  - application thread creation

- [`src/rfid.c`](./src/rfid.c)
  - RC522 bit-banged driver
  - UID reading
  - local authentication
  - local enrollment by card approach

- [`src/ui.c`](./src/ui.c)
  - local interface rendering
  - touch handling
  - idle screen
  - PIN screen
  - IP/network screen
  - access-result screen
  - profile/photo cache
  - runtime photos loaded or uploaded from web

- [`src/net.c`](./src/net.c)
  - embedded HTTP server
  - web admin login
  - dashboard
  - profile CRUD
  - tiled photo upload
  - profile import via JSON
  - access log page
  - web control for door and light
  - DHCP and NTP

- [`src/storage.c`](./src/storage.c)
  - in-RAM user database
  - persistence thread
  - write/read `users.json`
  - write/read `access.log`
  - write/read photos in separate binaries

- [`src/gpio.c`](./src/gpio.c)
  - door relay
  - light relay
  - physical buttons

## 5.2 Application threads

Defined in [`src/main.c`](./src/main.c):

- `ui`
- `rfid`
- `gpio`
- `net`

Threads disabled by current configuration:

- `tunnel`
- `status`

Relevant flags:

- `NETWORK_ISOLATION_MODE = 0`
- `NETWORK_HTTP_ONLY_MODE = 1`

## 5.3 Event queue

Modules communicate through `TX_QUEUE` in [`src/main.c`](./src/main.c).

Relevant events:

- `EVENT_RFID_AUTH_OK`
- `EVENT_RFID_AUTH_FAIL`
- `EVENT_DOOR_OPEN`
- `EVENT_LIGHT_ON`
- `EVENT_LIGHT_OFF`
- `EVENT_USER_ADDED`
- `EVENT_USER_REMOVED`
- `EVENT_CARD_REGISTERED`
- `EVENT_CARD_ALREADY_REGISTERED`
- `EVENT_CARD_REGISTRATION_FAILED`
- UI navigation events

Note:

- `EVENT_DOOR_CLOSE` still exists in enum, but automatic door closing no longer generates this event for logging

## 6. Main flows

## 6.1 Normal authentication flow

1. Board stays on `WAITING FOR USER / APPROACH CARD` screen.
2. RC522 detects a UID.
3. [`src/rfid.c`](./src/rfid.c) calls `storage_check_uid()`.
4. If the card exists:
   - updates current identity
   - opens the door
   - generates `EVENT_RFID_AUTH_OK`
5. If card does not exist:
   - updates current identity as not registered
   - generates `EVENT_RFID_AUTH_FAIL`
6. [`src/ui.c`](./src/ui.c) shows result screen for `10s`.
7. UI automatically returns to idle screen.

## 6.2 Local admin flow

1. On idle screen, user touches the gear icon.
2. UI opens numeric keyboard.
3. UI accepts:
   - the PIN of a registered administrator profile
   - the default fallback PIN `1234`
4. If PIN is correct, UI enters network/IP screen.
5. This screen shows:
   - DHCP state
   - current board IP
   - `Back` button

Note:

- the gear path screen is no longer a local enrollment screen; it is now a network information screen

## 6.3 Web admin flow

1. User opens the board HTTP interface.
2. Logs in with:
   - the PIN of a registered administrator profile
   - or the fallback PIN `1234`
3. Admin session remains valid for `10 minutes`.
4. From the session, user can:
   - list profiles
   - create/edit/remove profile
   - import profiles via JSON
   - upload photo
   - view access log
   - trigger door and light

## 7. Web interface

## 7.1 Network

The interface uses DHCP.

Current bring-up in [`src/net.c`](./src/net.c):

1. initialize `packet_pool`, `ip`, and `http_server`
2. create manual DHCP client in `g_net_dhcp_client`
3. request lease
4. wait for valid IP
5. start HTTP
6. try time sync via NTP

Important note:

- project includes local DHCP implementation in [`src/nxd_dhcp_client.c`](./src/nxd_dhcp_client.c)
- this file is part of the build and must not be removed

## 7.2 Main routes

User-visible routes:

- `/` - dashboard/home
- `/login` - web admin login
- `/logout` - ends admin session
- `/admin_profiles` - paginated profile list
- `/admin_profiles/<page>` - additional pages
- `/profile_form` - create profile
- `/profile_form/<id>` - edit profile
- `/upload_photo` - photo upload for profile
- `/import` - import profiles from JSON
- `/access_log` - view access log
- `/door` - door and light control

Action routes:

- `/add_user`
- `/remove_user`
- `/save_users`
- `/import_profiles`
- `/portaon`
- `/lampadatoggle`
- `POST /api/door/open`
- `/upload_photo_begin`
- `/upload_photo_chunk`
- `/upload_photo_commit`

## 7.3 Web interface characteristics

- split into small pages to reduce load on embedded HTTP server
- use of redirects and flash messages to avoid heavy responses
- profile page with pagination
- JSON import processed in background
- photo upload in small blocks

## 7.4 Web admin login

Current parameters:

- PIN: `1234`
- session validity: `10 minutes`

Implemented in [`src/net.c`](./src/net.c) with:

- validation against persisted administrator profiles
- `WEB_ADMIN_PIN` fallback only while no administrator PIN is configured in persisted profiles and the user list loaded without error
- session timeout in `WEB_ADMIN_SESSION_TICKS`

## 7.5 HTTP API for door opening

There is a dedicated route for external integrations:

- `POST /api/door/open`

Authentication accepts either of these headers:

- `X-API-KEY: <key>`
- `Authorization: Bearer <key>`

The key currently in use is the `API_KEY` constant defined in [`src/main.c`](./src/main.c). Before using this in production, the default value should be replaced with a private key of your own.

JavaScript example:

```js
await fetch("http://192.168.11.2/api/door/open", {
  method: "POST",
  headers: {
    "X-API-KEY": "<your-device-key>"
  }
});
```

Success response:

```json
{"ok":true,"message":"Door open command sent."}
```

## 7.6 HTTP API for meeting mode scheduling

Meeting mode can also be scheduled through the API. These routes use the same authentication as remote door opening:

- `POST /api/meeting/schedule`
- `POST /api/meeting/cancel`
- `GET /api/meeting/status`

The schedule endpoint accepts JSON or `application/x-www-form-urlencoded` forms. To schedule by relative delay, send `delay_seconds` and the `profile_indices` list with the authorized profile indexes:

```js
await fetch("http://192.168.11.2/api/meeting/schedule", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "X-API-KEY": "<your-device-key>"
  },
  body: JSON.stringify({
    delay_seconds: 300,
    profile_indices: [0, 4, 12]
  })
});
```

For an absolute time, use `start_unix`. In this case, the schedule depends on NTP being synchronized before the target time arrives:

```json
{"start_unix":1893456000,"profile_indices":[0,4,12]}
```

For daily recurrence, add `recurrence: "daily"`:

```json
{"start_unix":1893456000,"profile_indices":[0,4,12],"recurrence":"daily"}
```

For weekly recurrence, add `recurrence: "weekly"` and, optionally, `weekdays`. Weekdays use `0=Sunday`, `1=Monday`, ..., `6=Saturday`:

```json
{"start_unix":1893456000,"profile_indices":[0,4,12],"recurrence":"weekly","weekdays":[1,3,5]}
```

If `recurrence` is `"weekly"` and `weekdays` is omitted, the firmware automatically uses the weekday from `start_unix`.

Each schedule call returns an `id`. This `id` can be used to cancel only one pending meeting:

```js
await fetch("http://192.168.11.2/api/meeting/cancel", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "X-API-KEY": "<your-device-key>"
  },
  body: JSON.stringify({ id: 3 })
});
```

If `POST /api/meeting/cancel` is called without an `id`, all pending schedules are removed.

Important rules:

- each selected profile must exist and must have at least one registered card
- `delay_seconds` accepts up to 24 hours
- `delay_seconds` requires synchronized NTP because the firmware converts the delay to `start_unix` before saving
- up to 8 pending schedules can be saved at the same time
- pending schedules are persisted in QSPI in the `meeting.json` file
- recurring schedules keep the same `id` and update the next `start_unix` after each execution
- at the scheduled time, the firmware calls the same `storage_meeting_mode_start(...)` logic used by the web page

## 8. User data model

Defined in [`src/storage.h`](./src/storage.h):

```c
typedef struct
{
    char name[NAME_MAX_LEN];
    char role[STORAGE_ROLE_MAX_LEN];
    char chapter[STORAGE_CHAPTER_MAX_LEN];
    char photo_id[STORAGE_PHOTO_ID_MAX_LEN];
    bool is_admin;
    char admin_pin[STORAGE_ADMIN_PIN_MAX_LEN];
    unsigned int card_count;
    char cards[STORAGE_MAX_CARDS_PER_USER][UID_MAX_LEN];
} storage_user_profile_t;
```

Current limits:

- maximum users: `50`
- maximum cards per user: `4`
- runtime photo: up to `160x160`

## 9. QSPI/FileX persistence

## 9.1 General strategy

The project avoids touching QSPI too early during boot.

Because of this:

- storage only accesses media after a safe delay
- heavy load/save operations stay in a persistence thread
- application uses RAM as primary operational state

Relevant constants in [`src/storage.c`](./src/storage.c):

- `STORAGE_MEDIA_SAFE_DELAY_TICKS = 2s`
- `USERS_JSON_CHUNK_SIZE = 512`
- `USERS_JSON_OBJECT_SIZE = 1024`
- `ACCESS_LOG_BUFFER_SIZE = 4096`
- `ACCESS_LOG_ROTATE_SIZE = 64 KB`

## 9.2 Persisted files

Main files:

- `users.json`
- `access.log`
- `meeting.json`
- `photo_XXXXXXXX.bin`

### `users.json`

Stores persisted profiles.

The file is written in small fragments, one profile at a time, so persistence no longer depends on a single 8 KB JSON buffer. Writes use `users.tmp` and `users.bak` to avoid destroying the last valid file if a save fails midway. During boot, the parser also processes profile objects in chunks while keeping the operational `STORAGE_MAX_USERS` limit.

Current approximate format:

```json
[
  {
    "name": "Name",
    "uid": "PRIMARY_UID",
    "role": "ROLE",
    "chapter": "CHAPTER",
    "photo_id": "photo_or_asset",
    "is_admin": true,
    "admin_pin": "1234",
    "cards_csv": "UID1,UID2",
    "cards": ["UID1", "UID2"]
  }
]
```

Notes:

- `uid` exists for compatibility with legacy flows
- `is_admin` marks whether the profile can authenticate as administrator
- `admin_pin` stores the administrator numeric 4-digit PIN
- `cards_csv` was kept to reinforce parser compatibility
- `cards` is the richer format

### `access.log`

Stores persisted access log.

Each line is serialized as:

```text
unix_utc|event_type|data|user
```

Notes:

- writing is incremental, using append
- the active file rotates by size and uses `access.bak` as the rollover file
- boot reloads the persisted log tail by reading `access.bak` before `access.log`, preserving the order of the newest events

### `meeting.json`

Stores the pending meeting mode schedule queue.

Current approximate format:

```json
[
  {
    "id": 1,
    "start_unix": 1893456000,
    "recurrence": 2,
    "weekdays_mask": 42,
    "profiles": [0, 4, 12]
  }
]
```

Notes:

- writes use `meeting.tmp` and `meeting.bak` to avoid losing the previous file during a failed save
- the operational limit is `STORAGE_MEETING_SCHEDULE_MAX_ITEMS`, currently `8`
- `recurrence` uses `0=single`, `1=daily`, and `2=weekly`
- `weekdays_mask` uses bits from Sunday to Saturday; for example, `42` means Monday, Wednesday, and Friday
- single schedules are removed from the queue when their start time arrives
- recurring schedules stay in the queue and advance to the next future occurrence

### `photo_XXXXXXXX.bin`

Each persisted photo uses a file name derived from `photo_id`.

Content:

- custom header with magic, width, height, and payload size
- raw RGB565 payload

## 9.3 Automatic persistence

Today the project already requests persistence automatically in multiple flows:

- save profile
- remove profile
- local RFID enrollment
- profile import
- photo upload
- access log write

Main APIs in [`src/storage.h`](./src/storage.h):

- `storage_persist_now()`
- `storage_persist_wait()`
- `storage_access_log_enqueue()`
- `storage_access_log_persist_now()`
- `storage_photo_persist_now()`
- `storage_photo_ensure_loaded()`

Persistence states:

- `0 = idle`
- `1 = pending`
- `2 = success`
- `3 = failed`

## 10. User photos

Project works with two photo types:

### 10.1 Photos embedded in firmware

Assets in:

- [`src/assets/profile_photo_assets.h`](./src/assets/profile_photo_assets.h)
- [`src/assets/profile_photo_ids.h`](./src/assets/profile_photo_ids.h)

Use:

- good for stable photos
- high quality
- requires firmware rebuild

### 10.2 Photos uploaded from web

Current flow:

1. Browser crops image to square format.
2. It downsizes to `160x160`.
3. Converts to RGB565.
4. Splits into real `16` tiles of `40x40`.
5. Sends through:
   - `/upload_photo_begin`
   - `/upload_photo_chunk`
   - `/upload_photo_commit`
6. UI rebuilds image in RAM.
7. Profile receives `photo_id`.
8. Photo and profile are persisted in QSPI.

Relevant constants in [`src/net.c`](./src/net.c):

- `UPLOAD_IMAGE_DIM = 160`
- `UPLOAD_TILE_DIM = 40`

Relevant constants in [`src/ui.c`](./src/ui.c) and [`src/storage.h`](./src/storage.h):

- `UI_UPLOADED_PHOTO_SLOTS = 1`
- `STORAGE_RUNTIME_PHOTO_MAX_DIM = 160`

Notes:

- there is only `1` runtime loaded-photo slot to save RAM
- persisted photos can be reloaded on demand by UI
- `photo_id` is the link between profile, UI, QSPI, and assets

## 11. Local UI

## 11.1 Main screens

Main states in [`src/ui.c`](./src/ui.c):

- `UI_VIEW_IDLE`
- `UI_VIEW_RESULT`
- `UI_VIEW_PIN`
- `UI_VIEW_ENROLL_WAIT`

### Idle screen

- uses Ramo logo
- shows:
  - `WAITING FOR USER`
  - `APPROACH CARD`
- has gear icon to enter local admin flow

### Result screen

On authentication success or access denied:

- uses pixel-style font, aligned with idle screen
- tries to wrap long names into up to 2 lines
- shows `(ROLE-CHAPTER)` below name when data exists
- auto-returns after `10s`

### PIN screen

- local PIN: `1234`
- touch numeric keypad
- buttons `clear`, `0`, `enter`

### Network/IP screen

Internally called `UI_VIEW_ENROLL_WAIT`, but today its real use is:

- show network state
- show current IP
- show `WAITING FOR DHCP` if lease is not yet available

## 11.2 Screen dimming

After `60s` of inactivity:

- screen does not turn off
- framebuffer is re-rendered with approximately `30%` brightness

Important:

- this is not real backlight PWM
- it is software dimming over framebuffer
- chosen this way to avoid riskier timer/backlight changes

## 12. Software RTC and NTP

## 12.1 How it works

Project does not use a dedicated RTC.

Instead:

1. Network comes up via DHCP.
2. [`src/net.c`](./src/net.c) tries to obtain NTP server from DHCP option 42.
3. If there is no server in lease, it tries fallback:
   - `129.6.15.28`
   - `129.6.15.29`
4. Synchronized UTC time is delivered to [`src/main.c`](./src/main.c) through `app_time_set_utc()`.
5. From there, project keeps software clock using `tx_time_get()`.

## 12.2 Timestamp format

Access log uses:

- absolute time if NTP has already synchronized
- fallback `T+hh:mm:ss` if there is still no valid time

Current absolute timestamp is formatted as local time in [`src/main.c`](./src/main.c), with fixed `UTC-3` offset.

Displayed format:

```text
YYYY-MM-DD:HHhMMminSSs
```

Example:

```text
2026-03-19:20h03min05s
```

## 13. Access log

RAM capacity:

- `ACCESS_LOG_SIZE = 32`

Events currently recorded:

- authorized access
- denied access
- door opening
- card registration
- card already registered
- registration failure

Important:

- automatic door closing no longer appears in log

Web page:

- `/access_log`

Persistence:

- saved as `access.log` in QSPI with incremental append
- rotates to `access.bak` when the active file reaches the configured limit
- reloaded during boot with safe delay, reading the tail of the persisted files

## 14. Network

## 14.1 Current mode

- Ethernet enabled
- local HTTP enabled
- DHCP enabled
- NTP enabled
- `NETWORK_HTTP_ONLY_MODE = 1`

Not active in this state:

- remote tunnel
- remote status thread

## 14.2 Information shown to user

On web and local UI, project shows:

- current IP
- netmask
- link status
- network info via DHCP

## 14.3 Important note about DHCP

DHCP client was manually integrated into the project with local source:

- [`src/nxd_dhcp_client.c`](./src/nxd_dhcp_client.c)

This file must remain in the build.

## 15. Offline Firebase import

Project supports importing Firebase data without runtime dependency.

Main scripts in [`script`](./script):

- [`script/export_firebase_profiles.py`](./script/export_firebase_profiles.py)
- [`script/download_firebase_profile_photos.py`](./script/download_firebase_profile_photos.py)
- [`script/firebase_export_to_profiles.py`](./script/firebase_export_to_profiles.py)
- [`script/generate_card_bindings_template.py`](./script/generate_card_bindings_template.py)
- [`script/build_users_json_from_bundle.py`](./script/build_users_json_from_bundle.py)
- [`script/generate_profile_photo_assets.py`](./script/generate_profile_photo_assets.py)
- [`script/regenerate_profile_photo_assets.ps1`](./script/regenerate_profile_photo_assets.ps1)

Current local bundle:

- [`script/firebase_bundle`](./script/firebase_bundle)

Typical flow:

1. Export profiles from Firestore.
2. Download photos from Storage.
3. Downscale/convert photos.
4. Generate manifests or assets.
5. Fill RFID cards offline, if needed.
6. Generate `users.json` or import profiles via web interface.

## 16. Build and flashing

Expected environment:

- Renesas e2 studio
- configured GNU ARM toolchain
- project SSP/Synergy packages

Usual steps:

1. Open project in e2 studio.
2. Avoid `Generate Project Content` unless needed.
3. Build in `Debug`.
4. Flash firmware to board.

Main output:

- [`Debug/IoTRamoRenesas.elf`](./Debug/IoTRamoRenesas.elf)

Note:

- project contains generated code in `src/synergy_gen`
- manual changes in generated files or makefiles must be done carefully

## 17. Most important files for maintenance

- [`src/main.c`](./src/main.c)
- [`src/main.h`](./src/main.h)
- [`src/net.c`](./src/net.c)
- [`src/net.h`](./src/net.h)
- [`src/storage.c`](./src/storage.c)
- [`src/storage.h`](./src/storage.h)
- [`src/ui.c`](./src/ui.c)
- [`src/ui.h`](./src/ui.h)
- [`src/rfid.c`](./src/rfid.c)
- [`src/gpio.c`](./src/gpio.c)
- [`src/nxd_dhcp_client.c`](./src/nxd_dhcp_client.c)

## 18. Limitations and precautions

### 18.1 Memory

Project is sensitive to:

- large HTML buffers
- heavy HTTP responses
- large-body parsing inside HTTP callback
- excessive thread stack usage

Because of this:

- web was split into lightweight pages
- JSON import runs in background
- photo upload uses tiles
- several pages use `static` buffers

### 18.2 FileX/QSPI

Historically, this was the most sensitive area of the project.

Precautions:

- do not open media too early during boot
- do not move heavy save/load into HTTP callback
- keep persistence thread separate

### 18.3 Network

Even with stable DHCP/NTP today:

- large changes in `net.c` can reintroduce timeouts
- large HTTP uploads tend to break more easily

### 18.4 Photos

To keep current web upload quality:

- browser performs preprocessing
- board receives RGB565 tiles
- greatly increasing runtime resolution increases risk of RAM issues and timeouts

## 19. Quick validation checklist

After important changes, it is useful to validate:

1. Board boot.
2. Authorized and denied RFID reads.
3. Physical door opening.
4. `/login` page.
5. `/admin_profiles` page.
6. Profile creation/edit/removal.
7. Photo upload.
8. Board reboot and persistence confirmation.
9. `/access_log` page.
10. NTP time synchronization.

## 20. Auxiliary documents

For additional context:

- [`CURRENT_STATE_EN.md`](./CURRENT_STATE_EN.md): quick resume summary
- [`NETWORK_NOTES_EN.md`](./NETWORK_NOTES_EN.md): network stack investigation history

This `README_EN.md` should be treated as the main and most complete project documentation.
